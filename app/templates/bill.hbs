Look at this bill, yo.  See how whack this is:

<ul class="list-unstyled">
  {{#each bill in model}}

  <li><span class="muted">{{bill.bill_id}}</span></li>
  <li><span class="title">{{bill.official_title}}</span></li>
  <li><span class="muted">{{bill.description}}</span></li>
  <li><span class="muted">{{bill.sponsor_id}}</span></li>
  <li><span class="muted">{{bill.committee_ids}}</span></li>
  <li><span class="muted">{{bill.introduced_on}}</span></li>
  Links to this damn bill:
  <li><a href="{{bill.last_version.urls.html}}"><span class="muted">{{bill.last_version.urls.html}}</span></a></li>
  <li><a href="{{bill.last_version.urls.xml}}"><span class="muted">{{bill.last_version.urls.xml}}</span></a></li>
  <li><a href="{{bill.last_version.urls.pdf}}"><span class="muted">{{bill.last_version.urls.pdf}}</span></a></li>



  {{/each}}
</ul>
